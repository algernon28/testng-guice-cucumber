name: Test-method Suite
tests:
-  name: Test-Login EN FIREFOX
   parameters:
      browser: Firefox
#The following parameters are optional, the defaults are taken from Utils
      language: en
      country: US
      architecture: X64
      platform: WIN
      version: latest
   classes:
   - com.watermelon.steps.CucumberRunner